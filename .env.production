# Production build-time public env (Vite inlines VITE_-prefixed vars into the
# client bundle). These are PUBLIC values — the Clerk publishable key is safe to
# commit. Secrets (e.g. CLERK_SECRET_KEY) stay as Cloudflare Workers Builds
# secrets, never here.
#
# Clerk Frontend API host: clerk.mosaic.praveenjuge.com
VITE_CLERK_PUBLISHABLE_KEY=pk_live_Y2xlcmsubW9zYWljLnByYXZlZW5qdWdlLmNvbSQ
VITE_SITE_URL=https://mosaic.praveenjuge.com/
